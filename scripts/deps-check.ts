#!/usr/bin/env -S deno run --allow-read --allow-net --allow-run

// Goes beyond npm audit
// Checks TypeScript compatibility of updates
// Analyzes actual usage vs declared dependencies
// Suggests @types packages you might need